spring:
  application:
    name: recetas

  datasource:
    url: jdbc:mysql://localhost:3306/db_veterinaria_recetas?createDatabaseIfNotExist=true&serverTimezone=UTC
    username: root
    password:
      
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect

  flyway:
    enabled: false

server:
  port: 8084

servicios:
  mascotas:
    url: http://localhost:8082
  veterinarios:
    url: http://localhost:8087
  citas:
    url: http://localhost:8085

eureka:
  client:
    register-with-eureka: true
    fetch-registry: true
    service-url:
      defaultZone: http://localhost:8761/eureka/

springdoc:
  api-docs:
    enabled: true
  swagger-ui:
    enabled: true
    path: /swagger-ui.html
